# build folders
build/
out/
cmake-build-*/

# binaries
*.exe
*.out
*.app

# VS Code
.vscode/

# macOS
.DS_Store

# Each person makes their own local device_name.txt (we will .gitignore it so it’s not shared).
device_name.txt